<% if (typeEnum.length > 0) { %>import type { <% typeEnum.forEach(function (filed, index) { %><%=filed.typeEnumName%>, <% }); %> } from "@/types";<% } %>

export const <%=capitalSnakeFileName%> = {
<% typeEnum.forEach(function (filed, index) { %> 
  /** <%=filed.comment%> */
  <%=filed.upperCaseProp%>: [ <% filed.typeEnumArr.forEach(function (_filed, index) { %> 
    { value: <%=_filed.key%>, label: "<%=_filed.value%>" },<% }); %>
  ],
  /** <%=filed.comment%> */
  get<%=filed.firstUpperCaseProp%>(data: <%=filed.typeEnumName%>) {
    return (
      this.<%=filed.upperCaseProp%>.find((item) => {
        return item.value == data;
      })?.label ?? ""
    );
  },
<% }); %>
};



