export interface <%=typeName%> {<% fileds.forEach(function (filed, index) { %> 
  <% if (filed.typeEnum == true) { %>/** <%=filed.label%> */
  <% } %><%=filed.prop%>: <%=filed.ts%>;<% }); %>
}
<% typeEnum.forEach(function (filed, index) { %> 
/** <%=filed.comment%> */
export type <%=filed.key%> = <%=filed.value%>;
<% }); %>