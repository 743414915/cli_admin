import { <%=capitalSnakeFileName%> } from "@/enums/url-enums";
import type { <%=typeName%>, IPageModal } from "@/types";

const modalConfig: IPageModal<<%=capitalSnakeFileName%>, <%=typeName%>> = {
  url: {
    newUrl: <%=capitalSnakeFileName%>.NEW,
    editUrl: <%=capitalSnakeFileName%>.EDIT,
    listUrl: <%=capitalSnakeFileName%>.ALL,
  },
  header: {
    newTitle: "新建",
    editTitle: "编辑",
    labelWidth: "130px",
  },
  formItems: [
  <% fileds.forEach(function (filed, index) { %> 
    {
      type: "<%=filed.type%>",
      formItemBind: {
        prop: "<%=filed.prop%>",
        label: "<%=filed.label%>",
      },
      componentBind: {
        autosize: { minRows: 6 },
        type: "textarea",
        clearable: true,
        placeholder: "请输入<%=filed.label%>",
      }
    }<% if (index < fileds.length - 1) { %>,<% } %>
  <% }); %>
  ],
};

export default modalConfig;
